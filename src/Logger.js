export default class Logger{
    constructor() {
        
    }

    // test that is is called with the correct date and text
    logMessage(message) {
        // console log
        console.log(message);
    }
}